# Shared local database settings (used by docker compose + app)
POSTGRES_USER=trading
POSTGRES_PASSWORD=trading
POSTGRES_DB=trading_simulator
POSTGRES_PORT=5432

# App persistence mode: file | postgres | both
PERSISTENCE_MODE=postgres

# App connection string (host app running on your machine)
DATABASE_URL=postgres://trading:trading@localhost:5432/trading_simulator
